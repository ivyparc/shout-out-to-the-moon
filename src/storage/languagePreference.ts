import { LanguageCode } from "../i18n/languages";

export async function loadSavedLanguage(): Promise<LanguageCode | null> {
  return null;
}

export async function saveLanguage(_language: LanguageCode): Promise<void> {
  return undefined;
}
